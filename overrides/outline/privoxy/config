# Minimal privoxy config for HTTP->SOCKS5 forwarding via Outline

listen-address  0.0.0.0:8118

toggle  1
enable-remote-toggle 0
enable-remote-http-toggle 0
enable-edit-actions 0

# keepalive makes sense for n8n bursts, but do not set too high
keep-alive-timeout 5
socket-timeout 300

# Force ALL traffic through SOCKS5 (DNS via socks5)
# NOTE: final dot is required
forward-socks5 / outline:1080 .
